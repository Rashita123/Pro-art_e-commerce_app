import React from "react";
import {storiesOf} from "@storybook/react";

import {Input} from "../components/Input";

const stories=storiesOf('Input', module);

stories.add('Filled',()=>{
    return (<Input variant="filled"/>)
})

stories.add('flushed',()=>{
    return (<Input variant="flushed"/>)
})